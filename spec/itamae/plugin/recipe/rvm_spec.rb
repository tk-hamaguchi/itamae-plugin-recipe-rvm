require 'spec_helper'

describe Itamae::Plugin::Recipe::Rvm do
  it 'has a version number' do
    expect(Itamae::Plugin::Recipe::Rvm::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
